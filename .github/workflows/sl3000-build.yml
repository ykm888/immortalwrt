name: SL3000-Ultimate-Success-V12

on:
  workflow_dispatch:

env:
  WORKDIR: openwrt

jobs:
  build:
    runs-on: ubuntu-22.04
    permissions:
      contents: write
    steps:
      - name: 1. 检出仓库代码
        uses: actions/checkout@v4

      - name: 2. [延续修复] 解决磁盘与 Swap 锁定
        run: |
          sudo rm -rf /usr/share/dotnet /opt/ghc /var/lib/docker || true
          # 延续修复：彻底关闭 swap 防止 Text file busy
          sudo swapoff -a || true
          sudo rm -f /swapfile || true
          sudo fallocate -l 10G /swapfile
          sudo chmod 600 /swapfile
          sudo mkswap /swapfile
          sudo swapon /swapfile

      - name: 3. 安装依赖并克隆源码
        run: |
          sudo apt-get update
          sudo apt-get install -y build-essential flex bison m4 gawk rsync unzip wget python3 device-tree-compiler bc dos2unix libncurses5-dev libssl-dev
          git clone --depth=1 -b openwrt-24.10 https://github.com/immortalwrt/immortalwrt.git ${{ env.WORKDIR }}

      - name: 4. 执行注入脚本 (延续所有补丁)
        run: |
          chmod +x scripts/sl3000-patch.sh
          ./scripts/sl3000-patch.sh

      - name: 5. 编译 Host 工具链
        working-directory: ${{ env.WORKDIR }}
        run: |
          export PATH=/usr/bin:$PATH
          make tools/install -j$(nproc) || make tools/install -j1 V=s
          make toolchain/install -j$(nproc) || make toolchain/install -j1 V=s

      - name: 6. [终极加固] 地毯式 DTS 物理注入
        working-directory: ${{ env.WORKDIR }}
        run: |
          # 1. 延续修复：强制解压内核（解决 No such file 的根本）
          make target/linux/prepare -j$(nproc)
          
          # 2. 核心补丁：寻找【所有】可能的 Mediatek DTS 目录
          # 这会同时命中 toolchain 和 target 下的所有目录
          K_DTS_DIRS=$(find build_dir -path "*/arch/arm64/boot/dts/mediatek" -type d)
          echo "🔍 发现目标路径：$K_DTS_DIRS"

          if [ -z "$K_DTS_DIRS" ]; then
            echo "❌ 失败：未发现内核 DTS 目录！"
            exit 1
          fi

          # 3. 遍历注入
          for DTS_DIR in $K_DTS_DIRS; do
            echo "🎯 正在空投 DTS 到: $DTS_DIR"
            cp -fv "custom_files/mt7981b-sl3000-emmc.dts" "$DTS_DIR/"
            
            # 4. 延续修复：在 Makefile 中注册编译项
            K_MAKEFILE="$DTS_DIR/Makefile"
            if [ -f "$K_MAKEFILE" ]; then
              if ! grep -q "mt7981b-sl3000-emmc.dtb" "$K_MAKEFILE"; then
                echo 'dtb-$(CONFIG_ARCH_MEDIATEK) += mt7981b-sl3000-emmc.dtb' >> "$K_MAKEFILE"
              fi
            fi
          done

      - name: 7. [延续修复] 最终编译与镜像合成
        working-directory: ${{ env.WORKDIR }}
        run: |
          # 延续修复：彻底封锁 bison/m4 语法报错
          export BISON_PKGDATADIR=/usr/share/bison
          export M4=/usr/bin/m4
          export PATH=/usr/bin:${{ github.workspace }}/${{ env.WORKDIR }}/staging_dir/host/bin:$PATH
          
          # 最终生产
          make target/linux/compile -j$(nproc) V=s
          make target/install -j$(nproc) V=s
          
          # 产物校验
          if [ -z "$(ls bin/targets/mediatek/filogic/*sl3000-emmc-sysupgrade.bin 2>/dev/null)" ]; then
            echo "❌ 错误：固件依然没生成，请检查日志！"
            exit 1
          fi

      - name: 8. 发布 Release
        uses: softprops/action-gh-release@v2
        if: success()
        with:
          tag_name: SL3000-Final-Success-${{ github.run_id }}
          files: ${{ env.WORKDIR }}/bin/targets/mediatek/filogic/*sl3000-emmc-sysupgrade.bin
          token: ${{ secrets.GITHUB_TOKEN }}
